/** The name of the image that will exist in the artifact registry repo. */
export const IMAGE_NAME = 'app';

/** The name for the app that this code applies to. */
export const APP_NAME = 'layhold';

/** The name of the github repo containing the image to build. */
export const IMAGE_REPO = `${APP_NAME}/rails-app`;
